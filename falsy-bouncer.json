{"index.js":"function bouncer(arr) {\n  // Don't show a false ID to this bouncer.\n  let result = arr.filter(el => el === true ? el : el);\n  return result;\n}\n\nbouncer([7, \"ate\", \"\", false, 9]);"}